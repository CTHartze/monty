#include "monty.h"

/**
 * f_stack - Stack or queue is set, prints the top
 * @head: refers to stack head
 * @counter: refers to line_number
 * Return: void
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
